/* 
   // YOTAM APPROACH 

   Offered_user_id     / required for rating.
   recived_user_id    / required rating.
   donatation_user_id / required for linking the offer to the donation.
   wishlist_id / optional (needed only for automatic deletion).
   item:name, 
   item_details,
   
    // RODION APPROACH

   index_of_item_in_wish_list_id / (needed only for automatic deletion).
   item:name, 
   item_details,
   
*/